# Tasks
- [x] 任务 1：建立上游与 fork 的同步基线
  - [x] 确认当前仓库的 fork 远端（origin）与上游远端（upstream = YMFE/yapi）配置正确
  - [x] 明确 fork 的目标合入分支（如 `master`/`main`/维护分支）与分支保护/CI 门禁
  - [x] 建立 fork 侧约定：分支命名、PR 模板字段（upstream 链接/风险/验证）、合并方式（默认 squash/merge/rebase）

- [x] 任务 2：拉取并生成“未合并 PR”处理队列
  - [x] 获取 YMFE/yapi 的 Open PR 列表并纳入队列（包含 draft 标识与最后更新时间）
  - [x] 获取 YMFE/yapi 的 Closed 但未合并 PR 列表并纳入队列（以 merged/mergedAt 等字段判定）
  - [x] 对队列做最小分类（前端优先维度）：`client/build/ui`、`server`、`mixed`、`docs`、`deps`
  - [x] 为每个 PR 初始化处理状态（待评审/评审中/迁入中/验证中/已合入/拒绝迁入/需要补充信息）

- [x] 任务 3：制定评审清单与门禁（可执行）
  - [x] 定义代码评审维度与判定标准（架构/可维护性/安全/兼容/构建/业务影响）
  - [x] 定义前端验证命令集合与最低通过标准（lint/build/test/冒烟），以仓库现有脚本为准
  - [x] 定义“可拒绝迁入”的明确条件与记录方式（在 fork PR/issue 中留痕）

- [ ] 任务 4：逐项处理队列 PR（循环任务：对每个 PR 执行一次）
  - [x] 拉取 PR 代码与 diff（用于本地评审与验证），确认其基线分支与依赖关系
  - [ ] 严谨代码评审与业务审查，输出结论：通过/不通过/需要补充信息
  - [ ] 若通过：在 fork 创建对应分支与 PR（1 upstream PR ↔ 1 fork PR），并在 PR 描述中记录 upstream 链接、风险点与验证计划
  - [ ] 若需适配：在 fork 分支做最小修订以满足 fork 现状与质量门禁（不引入无关重构）
  - [ ] 运行验证门禁；失败则修复并复验；无法修复则更改结论为拒绝迁入并说明原因
  - [ ] 通过验证后合并该 fork PR，并关闭其处理状态

- [ ] 任务 5：收尾与质量回归
  - [ ] 对合入的 PR 做一次全量前端回归（构建 + 冒烟），确保跨 PR 组合无回归
  - [ ] 汇总迁入清单（已合入/拒绝迁入/需要补充信息）与关键风险点，便于后续维护跟进

# Task Dependencies
- 任务 2 依赖 任务 1
- 任务 3 依赖 任务 2
- 任务 4 依赖 任务 2、任务 3
- 任务 5 依赖 任务 4
