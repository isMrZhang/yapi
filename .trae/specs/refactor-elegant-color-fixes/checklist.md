- [x] 所有近期添加的硬编码颜色（如 `#333`, `#999`, `transparent`）已被移除并替换为对应的 SASS/LESS 变量（如 `$color-white`, `$color-bg-dark`）。
- [x] 基础全局样式（如 `body`, `html` 或 `#yapi` 根容器）已设置不透明的底色，防止操作系统深色模式导致的透明穿透。
- [x] 左侧边栏（如 `.group-bar` 等容器）的高度能够自然延伸到页面底部，无半截断层现象。
- [x] 页面在恢复深色或浅色主题变量后，字体颜色与背景色的对比度正常（如深色背景上白字）。