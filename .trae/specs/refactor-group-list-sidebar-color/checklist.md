- [x] 左侧边栏（`.group-bar`, `.m-sider`）已被正确重置为白底（`#fff`）样式，且与页面主体分离清晰。
- [x] 个人空间名称、简介、搜索框的文字在白底上颜色为深色（可读性高，如 `$color-text-dark`）。
- [x] 搜索框背景恢复为白色，不再出现深蓝色突兀。
- [x] 菜单悬停（Hover）和选中（Selected）时，未出现大面积的深蓝色强行覆盖背景，而是原生的 Ant Design 浅蓝或灰色反馈。
- [x] 代码已被彻底审核，没有多余的破坏性 `!important` 覆盖类残留。